import React from 'react';
import Layout from './Layout';

const Resume = () => {
    return (
        <div className="resume-container">
            <Layout />
        </div>

    )
};

export default Resume;